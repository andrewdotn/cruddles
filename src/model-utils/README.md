All source files in the `models` folder gets scanned by typeorm, and any classes
defined in them get treated as models.

This directory holds model-related code nearby, where typeorm won’t try to do
that.
